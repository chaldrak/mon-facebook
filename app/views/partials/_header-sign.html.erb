<nav class="navbar navbar-color">
	  <div class="container-fluid">
	    <div class="navbar-header">
	      <a class="navbar-brand" href="#">facebook</a>
	    </div>
	  	 <ul class="nav navbar-nav navbar-right">
		  <% if logged_in?%>
		  	<%= link_to "Déconnexion", session_path(current_user.id), method: :delete, class:"btn btn-danger navbar-btn"%>
		  <% else %>
			  	<button onclick="location.href='/sessions/new';" class="btn btn-default navbar-btn">Connexion</button>
	      		<button onclick="location.href='/users/new';" class="btn btn-success navbar-btn">Inscription</button>
	      <% end %>
	    </ul>
	  </div>
</nav>